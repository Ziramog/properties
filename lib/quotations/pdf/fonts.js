// Custom woff2 fonts fail on Vercel serverless due to fontkit parsing issues.
// Using Helvetica as a reliable fallback. The premium feel comes from layout,
// spacing, colors, and visual hierarchy — not the specific font family.
export const fontsLoaded = false;
